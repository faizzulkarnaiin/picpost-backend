export interface jwtPayload {
  id: number;
  nama: string;
  email: string;
}
export interface githubJwtPayload {
  id: number;
  client_id : string;
  nama: string;
  email: string;
}
export interface googleJwtPayload {
  id: number;
  client_id : string;
  nama: string;
  email: string;
}
