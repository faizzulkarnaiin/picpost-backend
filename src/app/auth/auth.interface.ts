interface jwtPayload {
  id: number;
  nama: string;
  email: string;
}
interface githubJwtPayload {
  id: number;
  client_id : string;
  nama: string;
  email: string;
}
interface googleJwtPayload {
  id: number;
  client_id : string;
  nama: string;
  email: string;
}
